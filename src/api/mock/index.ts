// In-memory adapter (step 4): store seeded from the prototype's data, the
// ten-state job machine with real guards, artificial latency and a forced
// failure toggle. It owns the state machine; screens send intents.
export {};
