// The contract, ported from the prototype in step 3: Category, Business,
// Technician, Job, EstimateItem, EventItem, statusLabels and the transition
// table. Money in integer cents, dates ISO 8601. No `any` in this folder.
export {};
